import numpy as np
import matplotlib.pylab as plt
from aux_functions import *

data = create_sinthetic_data(n_dim=2, N_vector=1024, n_cluster=4, max_range=16)

# X = np.vstack(data[:])
# print(X.shape)
# plt.plot(X[:, 0], X[:, 1], "om")
# plt.show()

